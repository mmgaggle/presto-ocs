apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: presto-service
  namespace: presto
spec:
  port:
    targetPort: 8080
  to:
    kind: Service
    name: presto-service
    weight: 100
  wildcardPolicy: None
